spring:
  application:
    name: appointment-management-system
  
  datasource:
    # Database connection - Environment variables kullanılıyor
    # Supabase için: Connection pooling endpoint kullanın (port 6543) veya direct connection için pool size'ı 1 yapın
    url: ${DB_URL:jdbc:postgresql://localhost:5432/appointment_db?sslmode=require}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      # Supabase ücretsiz plan: max 4 direct connections (Session mode)
      # ÖNEMLİ: Eğer Supabase kullanıyorsanız, connection pooling endpoint kullanın (port 6543)
      # veya pool size'ı 1'e düşürün
      maximum-pool-size: 1
      minimum-idle: 0
      # Connection timeout'u artırdık - Supabase bağlantıları için
      connection-timeout: 60000
      # Idle timeout'u kısalttık - bağlantıları daha hızlı serbest bırakmak için
      idle-timeout: 30000
      # Max lifetime'i kısalttık
      max-lifetime: 300000
      leak-detection-threshold: 60000
      pool-name: AppointmentHikariPool
      register-mbeans: true
      # Bağlantı sızıntılarını önlemek için
      connection-test-query: SELECT 1
      # Bağlantıları daha agresif şekilde kapat
      keepalive-time: 0
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    # Geçici olarak devre dışı - bağlantı sorunları çözülünce tekrar aktif edin
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: false
    out-of-order: true
    baseline-version: 0
    clean-disabled: true
  
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  secret: ${JWT_SECRET:}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:900000}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:604800000}

app:
  name: Smart Appointment Management System
  url: ${APP_URL:http://localhost:8080}
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  email:
    from: ${MAIL_FROM:noreply@appointment.com}
    verification-token-expiration: 86400000
    password-reset-token-expiration: 3600000

server:
  port: 8080

logging:
  level:
    root: ${LOG_LEVEL:INFO}
    com.project.appointment: ${LOG_LEVEL_APP:DEBUG}
    org.springframework.security: ${LOG_LEVEL_SECURITY:DEBUG}
  file:
    name: ${LOG_FILE:logs/appointment-management.log}
    max-size: 10MB
    max-history: 30
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
