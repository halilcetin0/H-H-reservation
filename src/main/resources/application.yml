spring:
  application:
    name: appointment-management-system
  
  datasource:
    url: jdbc:mysql://localhost:3306/appointment_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: james1232
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: hlt0rezervasyon@gmail.com
    password: harjmaxwdavimfpc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  secret: gorilemre42
  access-token-expiration: 900000  # 15 minutes in milliseconds
  refresh-token-expiration: 604800000  # 7 days in milliseconds

app:
  name: Smart Appointment Management System
  url: ${APP_URL:http://localhost:8080}
  email:
    from: ${MAIL_FROM:noreply@appointment.com}
    verification-token-expiration: 86400000  # 24 hours
    password-reset-token-expiration: 3600000  # 1 hour

server:
  port: 8080

logging:
  level:
    com.project.appointment: DEBUG
    org.springframework.security: DEBUG